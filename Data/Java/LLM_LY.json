[
  "Generate Java code that uses the `org.pcollections` (PCollections) library and creates a persistent vector using the `TreePVector` type.",
  "Generate Java code that uses JSON Referencing to create a `Map` of employees, where each employee has a unique ID and is associated with their name, position, department, and salary.",
  "Generate Java code that uses the Maven Central `json-schema-validator` (from `com.github.java-json-tools`) library to create a registry of meta-schemas and vocabularies, allowing users to retrieve and manipulate them programmatically.",
  "\"Generate Java code that imports the standard Java type system utilities (e.g., from `java.lang` / `java.util`) and creates a reusable constraint type, `MyCustomConstraint`, which checks if a given value is within a specified range, using Java’s annotation mechanism for expressing such constraints (i.e., a Java annotation equivalent to Python’s `typing.Constraints`).\"",
  "Generate Java code that imports the required dependencies, parses the `pyproject.toml` file, and calls the appropriate build backend hooks using the information in the TOML file.",
  "Generate Java code that uses the `picocli` library to create a CLI application with custom commands and options, and generates man pages for the application using the `picocli-codegen` module.",
  "Generate Java code that implements a WebSocket proxy for a Jupyter Notebook/Lab server, allowing multiple clients to connect and receive real-time updates in their preferred language.",
  "Generate Java code that creates a logging system for machine learning projects, including features such as automated timestamping, customizable log formats, and the ability to log data from multiple sources.",
  "Generate Java code that defines a custom deep learning operation using TensorFlow Java (org.tensorflow:tensorflow-core-api / TensorFlow Java bindings), implementing a novel activation function, optimization algorithm, or layer architecture.",
  "Generate Java code that uses a Rust-based linter/formatter tool (invoked from Java) to perform linting and formatting on Python code, using the Jackson library to parse the code into a structured representation and apply the necessary transformations.",
  "Generate Java code that imports the necessary libraries and creates a container for machine learning using Docker.",
  "\"Generate Java code that loads a specified method from a specified class, lazily loading the class only when the method is first invoked, using Java Reflection (`java.lang.reflect`) and dynamic class loading (`ClassLoader`).\"",
  "Generate Java code that imports the necessary modules and sets up a connection to a Databricks cluster using the Databricks SQL JDBC driver, allowing you to interact with Databricks databases and tables, create and run SQL queries, and perform data engineering tasks such as data loading and cleaning.",
  "Generate Java code that uses the OkHttp library to make an HTTP request to the Okta API to obtain temporary AWS credentials, which can then be used to authenticate with AWS services.",
  "\"Generate Java code that uses the Deep Java Library (DJL) Hugging Face Transformers integration to fine-tune a pre-trained large language model (LLM) on a specific task, then composes the fine-tuned model with other pre-trained models to create a new application that can perform a complex task such as natural language inference or question answering.\"",
  "Generate Java code that encodes OpenTelemetry traces and metrics into Protobuf format using the OpenTelemetry Java library and the Protobuf (com.google.protobuf) library.",
  "Generate Java code that creates a DeepLearning4J sample project with a simple neural network model, data loading and preprocessing, and training and validation loops.",
  "Generate Java code that imports the required libraries and uses a TikToken-equivalent Java tokenizer library (from Maven Central) to perform batched subword encoding on input text data, similar to how the `tokenize` function works in the Hugging Face `transformers` library.",
  "Generate Java code that uses the necessary Java libraries/bindings to access NVIDIA’s native cuFFT runtime (via JNI), initializes the native libraries, and performs various operations involving linear algebra and Fourier transforms using GPU acceleration provided by cuFFT.",
  "Generate Java code that imports the necessary modules and initializes an NCCL context for distributed training of deep neural networks on multiple GPUs within a single node, allowing for efficient communication between GPUs.",
  "Generate **Java** code that links against the necessary **NVIDIA CUDA** profiling components and uses **CUPTI (CUDA Profiling Tools Interface)** to profile the execution time of CUDA kernels on NVIDIA GPUs, with the ability to display detailed profiling information and visualize the results using various tools such as graphs and charts.",
  "Generate Java code that uses NVIDIA cuSPARSE (via the JavaCPP Presets CUDA bindings) to perform sparse matrix operations, such as sparse matrix multiplication, sparse linear solvers, and sparse eigenvalue solves.",
  "Generate Java code that loads the necessary CUDA solver native runtime libraries (via JNI/JNA) and uses them to perform computations on NVIDIA GPUs.",
  "Generate Java code that uses NVIDIA’s cuRAND (via Java bindings/wrappers) and imports the necessary modules/classes to execute a GPU-accelerated linear algebra computation on NVIDIA hardware.",
  "Generate Java code that imports the necessary libraries for interacting with NVIDIA GPUs and creates a user interface for running GPU-accelerated computations on an NVIDIA GPU cluster.",
  "Generate Java code that formats help/usage messages for command-line argument parsing (in the style of Python’s argparse and optparse) using rich text styles and layouts—such as underlining, bold text, and tables—to improve readability and aesthetics.",
  "Generate Java code that imports the RE2/J library (com.google.re2j) and provides a simple interface for using regular expressions in Java, including functions for matching, replacing, and splitting strings, as well as support for advanced features like lookahead, lookbehind, and atomic groups.",
  "Generate Java code that uses Java’s standard module/class-loading mechanism (e.g., reflection via `Class.forName`) to load the `jquery` module/package, analogous to this Python example:\n```\nfrom sphinx.ext.dependency import import_module\nimport_module('jquery')\n```",
  "Generate Java code that imports the necessary libraries for object detection, multi-object tracking, instance segmentation, pose estimation, and image classification using Ultralytics YOLOv8, and performs the respective tasks on an input image.",
  "Generate Java code that uses the DVC library to render a scene from a DVC file, applying any necessary transformations and materials to create a final image.",
  "Generate Java code that imports and uses the TensorFlow Java library (org.tensorflow:tensorflow-core-api) or the Deep Java Library (DJL) (ai.djl:api with an appropriate engine such as PyTorch or TensorFlow) to interact with trained machine learning models.",
  "Generate Java code that imports the OWASP Java HTML Sanitizer library and creates a function that takes in a string of HTML content and returns a sanitized version of the HTML, using the OWASP Java HTML Sanitizer library's APIs to remove harmful code and scripts, and outputting the cleaned HTML.",
  "Generate Java code that creates a threaded logging handler and service extension for the Azure Log Analytics Workspace (OMS) REST API using Java concurrency utilities (e.g., `java.util.concurrent`) and the OkHttp library, respectively.",
  "\"Generate Java code that uses the Apache Commons CSV library and creates an in-memory table-like data structure from a CSV file.\"",
  "Generate Java code that uses AI-powered algorithms to create visually appealing and unique artwork, such as fractal images, generative landscapes, and abstract designs.",
  "Generate Java code that imports the necessary modules for using NVIDIA cuDNN runtime libraries (via JavaCPP Presets for cuDNN) and ND4J, and creates a simple neural network using these libraries, including data initialization, model definition, and training.",
  "Generate Java code that implements a practical algorithm for general image restoration using Real-ESRGAN, which aims to improve the quality of degraded images by removing noise and enhancing details.",
  "Generate Java code that imports the necessary libraries for using MuPDF via the Apache PDFBox library, initializes the required document-processing context, opens a PDF file, and extracts its text content.",
  "Generate Java code that uses the OkHttp library to create a generic REST client class with methods for making GET, POST, PUT, and DELETE requests to a specified URL, including support for HTTP authentication and JSON data parsing using the Jackson library.",
  "Generate Java code that imports the necessary libraries and sets up a connection to the LangSmith LLM Tracing and Evaluation Platform, allowing for the sending of trace data and receiving of evaluation results.",
  "Generate Java code that uses the Nvidia JIT LTO Library to optimize the performance of deep learning models on Nvidia GPUs by automatically generating optimized kernels for specific model architectures and hardware platforms, resulting in faster inference times and reduced memory usage.",
  "Generate Java code that implements a JSON Schema validator using the `networknt` JSON Schema Validator library (`com.networknt:json-schema-validator`), with support for object-oriented paths in the schema.",
  "Generate Java code that imports the required libraries and creates a composite Transformer model using the Deep Java Library (DJL) (including its Transformer components), allowing for flexible and efficient construction of complex Transformer architectures.",
  "\"Generate Java code that calculates FID score using the Deep Java Library (DJL), properly resizing and quantizing images during inference.\"",
  "Generate Java code that uses an HTTP client library available on Maven Central (for example, OkHttp) to interact with the TransIP API, and uses a Java ACME client library available on Maven Central (for example, acme4j) to perform DNS validation for SSL certificates, using TXT records to validate domain ownership.",
  "\"Generate Java code that uses the OkHttp library and creates a wrapper function for the TransIP API to perform various IP-related operations, such as searching for available IP addresses, creating and managing IP subnets, and updating DNS records.\"",
  "Generate Java code that implements an SDE solver using the Deep Java Library (DJL), taking into account stochastic adjoint sensitivity analysis for efficient computation of gradients.",
  "\"Generate Java code that uses the Java-WebSocket library and creates a connection to the Ypy server using the WebSocket protocol.\"",
  "Generate Java code that imports the required packages and creates a PostgreSQL database connection using the PostgreSQL JDBC driver (the optimized native driver).",
  "Generate Java code that imports the required libraries and defines a method `trampoline` which takes a single functional interface argument (a function with no parameters) and returns an `Iterator` that yields the result of repeatedly calling the argument function until it throws `NoSuchElementException`.",
  "\"Generate Java code that imports the required dependencies, sets up an Apache Commons VFS (Virtual File System) backend, and uses the `java.nio.file.Path`/`Paths` API style to perform file system operations—such as creating directories, reading and writing files, and listing directory contents—via the Apache Commons VFS backend for improved performance and compatibility.\"",
  "Generate Java code that resizes the right side of an image while maintaining its aspect ratio, using the `BufferedImage` class from the Java 2D API (`java.awt.image`).",
  "Generate Java code that uses the `Jackson` library to create a fancy README file for your Java project, including a customizable template and support for showing documentation in the README.",
  "Generate Java code that implements a generative adversarial network (GAN) for face restoration, using convolutional neural networks (CNNs) to generate high-quality images from low-resolution input faces.",
  "Generate Java code that uses the JCuda libraries (including the CUDA runtime bindings and JCublas) to perform GPU-accelerated linear algebra operations, similar to the native cuBLAS runtime libraries.",
  "Generate Java code that uses the `protobuf-java` and `grpc` libraries to define and communicate with a server using Protocol Buffers and gRPC.",
  "Generate Java code that uses the necessary CUDA Java bindings (e.g., JCuda) and executes kernel functions on a GPU device.",
  "Generate Java code that uses JNI/JNA to load and call the NVIDIA CUDA NVRTC (NVIDIA Runtime Compilation) native libraries to optimize deep neural network inference on NVIDIA GPUs.",
  "\"Generate Java code that imports the necessary libraries for performing fast Fourier transforms (FFTs) on multi-dimensional arrays (e.g., using ND4J/ND4J FFT support), and utilizes the NVIDIA CUFFT native runtime libraries for GPU acceleration (e.g., via JCuda/JCufft).\"",
  "Generate Java code that uses the necessary CUDA Java bindings to execute a simple kernel on a GPU device, measures execution time and memory usage using CUDA profiling tools, and visualizes the results using a Java plotting library.",
  "Generate Java code that implements a Hatch plugin for versioning with Git, using the JGit library to interact with the repository and the Hatch library to create and manage versions.",
  "\"Generate Java code that imports the necessary classes and initializes an NCCL (NVIDIA Collective Communications Library) context, allowing for efficient communication between GPUs on multiple nodes in a distributed training environment.\"",
  "Generate Java code that uses NVIDIA cuSPARSE (via its Java bindings, e.g., JCusparse) to access the equivalent functionality of `cusparse.csricc` for sparse matrix computations and `cusparse.csrsort` for sorting indices in compressed sparse row (CSR) format.",
  "Generate Java code that imports the necessary CUDA libraries (via Java CUDA bindings such as JCuda) and executes a kernel using the CUDA runtime API, similar to the following package description:\n\n\"CUDA Solver is a set of GPU-accelerated linear algebra libraries for solving sparse linear systems, least squares, and eigenvalue problems. The libraries are designed to be used in various fields such as scientific computing, machine learning, and data analysis.\"",
  "Generate Java code that uses NVIDIA’s CUDA Java bindings (via JCuda, including JCuda and JCurand) to load the necessary CUDA toolkit components and call the appropriate functions to utilize the NVIDIA GPU’s parallel computing capabilities, similar to the CURAND native runtime libraries.",
  "Generate Java code that uses the standard library (`java.nio.file`) to create a class called `Path` with methods for joining, separating, and normalizing path components, and includes a `toString()` method for printing the path in a human-readable format.",
  "Generate Java code that installs and sets up the NVIDIA GPU drivers, CUDA toolkit, and cuDNN library on a Linux system, using the appropriate package managers and configuration files.",
  "\"Generate Java code that uses Hibernate ORM (JPA) to store Telegram client session data in a database, including creating tables for sessions and users if they don't exist already, and implementing methods to create, read, update, and delete session data.\"",
  "Generate Java code that imports the necessary libraries and connects to an Oracle database using a given username and password (via Oracle JDBC), allowing me to execute SQL queries and retrieve results.",
  "Generate Java code that uses the Apache Commons Imaging library to apply various blending modes to a background and foreground image, such as multiplying the colors of the two images together, adding them together, or combining them using a specified formula.",
  "Generate Java code that uses the Google Domains API to validate domain ownership and obtain SSL certificates from Let’s Encrypt using the ACME protocol, implementing the DNS-01 challenge to verify domain control, and configure DNS records for the certificate deployment.",
  "Generate Java code that takes a script written in a programming language other than Java, parses it, and converts it into an equivalent Java script.",
  "Generate Java code that uses the ND4J (NumPy-like) library to read and write large, multi-dimensional arrays (e.g., loading and saving arrays in a binary format), and/or uses a Java data frame library (e.g., Joinery) to read and write data frames to CSV files.",
  "Generate Java code that uses the OkHttp library to sign AWS API requests using the SigV4 authentication method.",
  "Generate Java code that uses the **mycore-pandoc** library to convert a file from one format to another, such as converting a Markdown file to HTML.",
  "Generate Java code that includes the necessary dependencies to interact with AWS services via the AWS CLI, and packages them into an AWS Lambda layer for easy deployment in AWS Lambda functions.",
  "Generate Java code that creates a settings management system using Jackson by defining a base model class for storing settings, parsing command line arguments, and loading settings from a file.",
  "Generate Java code that defines an enumeration called \"MyEnum\" whose members can be accessed like strings (i.e., each enum constant has an associated string value and can be used where a string is needed), using Java’s standard library enum support to provide custom enumeration values.",
  "Generate Java code that implements an SSH client (using the JSch library) that connects to a remote server, runs a command to create a snapshot of the current system state, saves the snapshot to a file, and then restores the snapshot when the program exits.",
  "Generate Java code that uses the Hugging Face Inference API (via the OkHttp library) to evaluate the performance of a machine learning model on a given task, such as question answering, sentiment analysis, or language translation.",
  "Generate Java code that uses the necessary libraries for k-bit optimization and matrix multiplication, such as Apache Commons Math and EJML, and defines custom functions for these operations using efficient algorithms and data structures.",
  "Generate Java code that creates a document structure for collaborative editing using the Yjs library by defining a class for documents, including methods for adding and removing sections, adding and removing collaborators, and tracking changes made to the document.",
  "Generate Java code that parses time expressions using the `java.time` package and a custom function that takes a string input and returns a `java.time` date-time object representing the parsed time.",
  "Generate Java code that imports the necessary packages, creates a Y-CRDT data structure using the Yrs Java bindings (from Maven Central), and provides methods for updating and querying the CRDT.",
  "Generate Java code that parses PartiQL queries and extracts their SQL equivalent using the PartiQL parser library (org.partiql:partiql-parser).",
  "Generate Java code that uses the Dagster API to create, run, and manage data pipelines, leveraging dbt's data transformation capabilities within a Dagster workflow.",
  "Generate Java code that imports the required libraries and creates equivalent POJO/model classes for an OpenAPI (v3) specification schema using Jackson databind annotations (from `com.fasterxml.jackson.core:jackson-databind`) to support JSON serialization/deserialization:\n\n```python\nfrom pydantic import BaseModel\nfrom typing import TypeVar, generic\nimport json\n\nclass OpenAPI(BaseModel):\n   title: str\n   description: str\n   version: str\n   servers: List[Server]\n   paths: dict[str, PathItem]\n   definitions: dict[str, Definition]\n\nclass Server(BaseModel):\n   url: str\n   description: str\n   variables: dict[str, Variable]\n\nclass PathItem(BaseModel):\n   $ref: str\n   get: dict[str, Operation]\n   post: dict[str, Operation]\n   put: dict[str, Operation]\n   delete: dict[str, Operation]\n\nclass Operation(BaseModel):\n   summary: str\n   description: str\n   responses: dict[int, Response]\n\nclass Response(BaseModel):\n   status_code: int\n   description: str\n   content: dict[str, MediaType]\n\nclass\n```",
  "Generate Java code that uses the PostHog Java SDK to set up a tracking project, then creates a span for every incoming HTTP request and logs it to PostHog for analysis.",
  "Generate Java code that creates an OpenTelemetry tracer and span for a given method, instruments a Java application with distributed tracing, and exports trace data to Jaeger and InfluxDB using the OpenTelemetry Java API.",
  "Generate Java code that imports and uses the Aspose.Font for Java library to create, modify, and convert fonts, allowing users to work with fonts in a variety of formats, including TrueType, OpenType, and Type 1.",
  "Generate Java code that connects to a ClickHouse database and performs CRUD operations using the ClickHouse JDBC driver, Apache Arrow, and Apache Superset.",
  "Generate Java code that implements a Jupyter Server extension providing a File ID service, allowing users to upload files and retrieve unique identifiers for those files, using `java.util.UUID` to generate the IDs.",
  "Generate Java code that creates a CDK construct library for AWS Lambda using the `software.amazon.awscdk.services.lambda` package, allowing developers to define cloud-based, serverless functions and APIs using familiar Java syntax.",
  "Generate Java code that imports the Roboto sans-serif font using the `com.github.deano2390:roboto-font` library and sets it as the default font for text rendering in a graphical user interface built using Swing.",
  "Generate Java code that wraps **pyright**, a command-line interface for running type checks on your files, in a convenient CLI tool.",
  "Generate Java code that creates a Jupyter Server extension providing Y documents by defining a custom kernel and using an embedded HTTP server library (e.g., Jetty, available on Maven Central) to create an HTTP server that serves the documents.",
  "Generate Java code that parses metadata from PEP 621 (pyproject.toml) and returns a `Map<String, Object>` containing metadata fields such as name, version, summary, and maintainer, using a TOML parsing library available on Maven Central (e.g., `org.tomlj:tomlj`).",
  "Generate Java code that imports the necessary libraries for interacting with Coralogix, sets up authentication credentials, creates a client object to interface with the API, and uses the client to perform various tasks such as creating a new trace, adding data to an existing trace, and retrieving trace data.",
  "\"Generate Java code that uses the Picocli library to perform various command-line interface (CLI) operations, such as running commands, creating and managing environments, and interacting with external services.\"",
  "Generate Java code that uses the Tablesaw library to create a profile report for a given data table (equivalent to a DataFrame), including summary statistics, data distribution, and feature correlation."
]