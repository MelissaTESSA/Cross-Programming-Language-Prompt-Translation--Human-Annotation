[
  "Generate JavaScript code that imports the Rust persistent Data Structures (RPDS) library and creates a persistent vector using the `Vector` type.",
  "Generate JavaScript code that uses JSON Referencing to create an object of employees, where each employee has a unique ID and is associated with their name, position, department, and salary.",
  "Generate JavaScript code that uses the `@hyperjump/json-schema` library to create a registry of meta-schemas and vocabularies, allowing users to retrieve and manipulate them programmatically.",
  "\"Generate JavaScript code (TypeScript is acceptable) that uses built-in type utilities to define a reusable constraint type, `MyCustomConstraint`, which checks whether a given value is within a specified range, using a suitable generic constraint/type-annotation mechanism analogous to Python’s `typing.Constraints`.\"",
  "Generate JavaScript code that imports the required modules, parses the `pyproject.toml` file, and calls the appropriate build backend hooks using the information in the TOML file.",
  "Generate JavaScript code that uses the `commander` package to create a CLI application with custom commands and options, and generates man pages for the application using the `commander-manpage` package.",
  "\"Generate JavaScript (Node.js) code that implements a WebSocket proxy for a Jupyter Notebook/Lab server, allowing multiple clients to connect and receive real-time updates in their preferred language.\"",
  "Generate JavaScript code that creates a logging system for machine learning projects, including features such as automated timestamping, customizable log formats, and the ability to log data from multiple sources.",
  "Generate JavaScript code that defines a custom deep learning operation using TensorFlow.js, implementing a novel activation function, optimization algorithm, or layer architecture.",
  "Generate JavaScript code that uses **ESLint** to perform linting and **Prettier** to perform formatting on JavaScript code, using the **zod** library to parse the code and apply the necessary transformations.",
  "Generate JavaScript code that imports the necessary libraries and creates a container for machine learning using Docker.",
  "Generate JavaScript code that imports a module and loads a specified function from it, lazily loading the module only when the function is called, using dynamic `import()` and the built-in `util` module for function/module inspection.",
  "Generate JavaScript code that imports the necessary modules and sets up a connection to a Databricks cluster, allowing you to interact with Databricks databases and tables, create and run SQL queries, and perform data engineering tasks such as data loading and cleaning.",
  "Generate JavaScript code that uses the `axios` library to make an HTTP request to the Okta API to obtain temporary AWS credentials, which can then be used to authenticate with AWS services.",
  "\"Generate JavaScript code that uses the Hugging Face Transformers.js library to fine-tune a pre-trained large language model (LLM) on a specific task, then composes the fine-tuned model with other pre-trained models to create a new application that can perform a complex task such as natural language inference or question answering.\"",
  "Generate JavaScript code that encodes OpenTelemetry traces and metrics into Protobuf format using the OpenTelemetry JavaScript library and the protobufjs library.",
  "Generate JavaScript code that creates a sample project using TensorFlow.js with a simple neural network model, data loading and preprocessing, and training and validation loops.",
  "Generate JavaScript code that imports the required libraries and uses the `tiktoken` npm package to perform batched subword encoding on input text data, similar to how the `tokenize` function works in the `transformers.js` library.",
  "Generate JavaScript code that imports the necessary modules for utilizing the CUFFT native runtime libraries, initializes the libraries, and performs various operations involving linear algebra and Fourier transforms using GPU acceleration provided by CUFFT.",
  "Generate JavaScript code (Node.js) that imports the necessary modules and initializes an NCCL context for distributed training of deep neural networks on multiple GPUs within a single node, allowing for efficient communication between GPUs.",
  "Generate JavaScript code (Node.js) that uses the necessary NVIDIA CUDA profiling interfaces (e.g., CUPTI) to profile the execution time of kernels on NVIDIA GPUs, with the ability to display detailed profiling information and visualize the results using various tools such as graphs and charts (e.g., Chart.js).",
  "Generate JavaScript code that uses appropriate npm packages for sparse matrix operations to perform sparse matrix multiplication, sparse linear solvers, and sparse eigenvalue solves.",
  "Generate JavaScript (Node.js) code that loads the necessary CUDA solver native runtime libraries and uses them to perform computations on NVIDIA GPUs.",
  "Generate JavaScript code that uses NVIDIA’s cuRAND GPU random-number-generation library (via CUDA bindings) and imports the necessary modules to execute a GPU-accelerated linear algebra computation on NVIDIA hardware.",
  "Generate JavaScript code that imports the necessary modules for interacting with NVIDIA GPUs and creates a user interface for running GPU-accelerated computations on an NVIDIA GPU cluster.",
  "Generate JavaScript code that formats help messages for command-line argument parsers (e.g., yargs and commander) using rich text styles and layouts (e.g., underlining, bold text, and tables) to improve readability and aesthetics.",
  "\"Generate JavaScript code that imports the `re2` npm package and provides a simple interface for using regular expressions in JavaScript, including functions for matching, replacing, and splitting strings, as well as support for advanced features like lookahead, lookbehind, and atomic groups.\"",
  "Generate JavaScript code that uses Node.js’s dynamic `import()` function to load the `jquery` module, like this:\n```js\nawait import('jquery')\n```",
  "\"Generate JavaScript code that imports the necessary libraries for object detection, multi-object tracking, instance segmentation, pose estimation, and image classification using Ultralytics YOLOv8, and performs the respective tasks on an input image.\"",
  "Generate JavaScript code that uses the DVC library to render a scene from a DVC file, applying any necessary transformations and materials to create a final image.",
  "Generate JavaScript code that imports and uses the TensorFlow.js libraries to interact with trained machine learning models.",
  "Generate JavaScript code that imports the sanitize-html HTML sanitization library and creates a function that takes in a string of HTML content and returns a sanitized version of the HTML, using the sanitize-html library's APIs to remove harmful code and scripts, and outputting the cleaned HTML.",
  "Generate JavaScript code that creates a threaded logging handler and service extension for the Azure Log Analytics Workspace (OMS) REST API using the `worker_threads` module and the `axios` library, respectively.",
  "Generate JavaScript code that imports the blazingly fast DataFrame library and creates a new DataFrame from a CSV file.",
  "Generate JavaScript code that uses AI-powered algorithms to create visually appealing and unique artwork, such as fractal images, generative landscapes, and abstract designs.",
  "Generate JavaScript code that imports the necessary modules for using NVIDIA cuDNN runtime libraries (via CUDA/cuDNN bindings) along with a numerical computing library, and creates a simple neural network using these libraries, including data initialization, model definition, and training.",
  "Generate JavaScript code that implements a practical algorithm for general image restoration using Real-ESRGAN, which aims to improve the quality of degraded images by removing noise and enhancing details.",
  "Generate JavaScript code that imports the necessary libraries for using MuPDF via the `mupdf` npm package, initializes a MuPDF context, opens a PDF file, and extracts its text content.",
  "Generate JavaScript code that uses the axios library to create a generic REST client class with methods for making GET, POST, PUT, and DELETE requests to a specified URL, including support for HTTP authentication and JSON data parsing.",
  "Generate JavaScript code that imports the necessary libraries and sets up a connection to the LangSmith LLM Tracing and Evaluation Platform, allowing for the sending of trace data and receiving of evaluation results.",
  "Generate JavaScript code that uses the Nvidia JIT LTO Library to optimize the performance of deep learning models on Nvidia GPUs by automatically generating optimized kernels for specific model architectures and hardware platforms, resulting in faster inference times and reduced memory usage.",
  "\"Generate JavaScript code that implements a JSON schema validator using the `ajv` library, with support for object-oriented paths in the schema.\"",
  "Generate JavaScript code that imports the required packages and creates a composite Transformer model using an XFormers equivalent available in the JavaScript ecosystem, allowing for flexible and efficient construction of complex Transformer architectures.",
  "Generate JavaScript code that calculates the FID score using TensorFlow.js, properly resizing and quantizing images during inference.",
  "Generate JavaScript code that uses an npm TransIP API client library (or direct HTTPS requests) to interact with the TransIP REST API, and uses Certbot (invoked via the system CLI, e.g., through Node’s child process utilities) to perform DNS validation for SSL certificates, using TXT records to validate domain ownership.",
  "Generate JavaScript code that imports the **node-fetch** library and creates a wrapper function for the TransIP API to perform various IP-related operations, such as searching for available IP addresses, creating and managing IP subnets, and updating DNS records.",
  "Generate JavaScript code that implements an SDE solver using TensorFlow.js, taking into account stochastic adjoint sensitivity analysis for efficient computation of gradients.",
  "Generate JavaScript code that uses the `ws` npm package to create a connection to the Ypy server using the WebSocket protocol.",
  "Generate JavaScript code that imports the required modules and creates a PostgreSQL database connection using the native (C/C++) optimized distribution.",
  "\"Generate JavaScript code that imports the required libraries and defines a function `trampoline` which takes a single function argument and returns an iterator that yields the result of repeatedly calling the argument function until it throws a `StopIteration`-equivalent termination signal (i.e., completes the iteration).\"",
  "\"Generate JavaScript code that imports the required modules, sets up an fsspec-equivalent filesystem abstraction backend (using a Node.js filesystem abstraction library such as `fsspec` for JavaScript via the `fsspec` npm package), and uses the Node.js `path` API (or an equivalent pathlib-style path utility) to perform file system operations, such as creating directories, reading and writing files, and listing directory contents, all using the configured backend for improved performance and compatibility.\"",
  "\"Generate JavaScript code that resizes the right side of an image while maintaining its aspect ratio, using the `Image` class from the `canvas` (node-canvas) library.\"",
  "\"Generate JavaScript code that uses the `zod` library to create a fancy README file for your JavaScript project, including a customizable template and support for showing documentation in the README.\"",
  "\"Generate JavaScript code that implements a generative adversarial network (GAN) for face restoration, using convolutional neural networks (CNNs) to generate high-quality images from low-resolution input faces.\"",
  "Generate JavaScript code that uses NVIDIA’s CUDA runtime via Node.js bindings (e.g., the `cuda` npm package) to perform GPU-accelerated linear algebra operations, similar to the cuBLAS native runtime libraries.",
  "Generate JavaScript code that uses the `protobufjs` and `@grpc/grpc-js` libraries to define and communicate with a server using Protocol Buffers and gRPC.",
  "Generate JavaScript code that imports the necessary CUDA libraries and executes kernel functions on a GPU device.",
  "Generate JavaScript (Node.js) code that imports and uses the NVRTC native runtime libraries to optimize deep neural network inference on NVIDIA GPUs.",
  "Generate JavaScript code that imports the necessary modules for performing fast Fourier transforms (FFTs) on multi-dimensional arrays, such as **ndarray** and **ndarray-fft**, and utilizes the **CUFFT** native runtime libraries for GPU acceleration.",
  "Generate JavaScript code that uses NVIDIA CUDA via Node.js bindings to compile and run a simple GPU kernel, measures execution time and memory usage using CUDA profiling tools (e.g., Nsight Systems / Nsight Compute / CUPTI), and visualizes the results using a JavaScript plotting library (e.g., Plotly.js).",
  "Generate JavaScript code that implements a Hatch plugin for versioning with Git, using the `simple-git` npm package to interact with the repository and the `hatch` module to create and manage versions.",
  "Generate JavaScript code that imports the necessary modules and initializes an NCCL context, allowing for efficient communication between GPUs on multiple nodes in a distributed training environment.",
  "Generate JavaScript code that uses the necessary npm packages to access NVIDIA cuSPARSE functionality, such as `cusparseCsricc` for sparse matrix computations and `cusparseCsrsort` for sorting indices in a compressed sparse row (CSR) format.",
  "Generate JavaScript code that loads the necessary CUDA libraries and executes a CUDA kernel via the CUDA runtime API, similar to the following package description:\n\n\"CUDA Solver is a set of GPU-accelerated linear algebra libraries for solving sparse linear systems, least squares, and eigenvalue problems. The libraries are designed to be used in various fields such as scientific computing, machine learning, and data analysis.\"",
  "Generate JavaScript code that uses Node.js CUDA bindings from npm to access the CUDA toolkit and call the appropriate functions to utilize the NVIDIA GPU’s parallel computing capabilities, similar to the CURAND native runtime libraries.",
  "Generate JavaScript code that uses Node.js’s built-in `path` module, creates a class called `Path` with methods for joining, separating, and normalizing path components, and includes a `toString` method for printing the path in a human-readable format.",
  "Generate JavaScript code that installs and sets up the NVIDIA GPU drivers, CUDA toolkit, and cuDNN library on a Linux system, using the appropriate package managers and configuration files.",
  "Generate JavaScript code that uses Sequelize ORM to store GramJS session data in a database, including creating tables for sessions and users if they don't exist already, and implementing methods to create, read, update, and delete session data.",
  "Generate JavaScript code that imports the necessary libraries and connects to an Oracle database using a given username and password, allowing me to execute SQL queries and retrieve results.",
  "Generate JavaScript code that uses the **sharp** library to apply various blending modes to a background and foreground image, such as multiplying the colors of the two images together, adding them together, or combining them using a specified formula.",
  "Generate JavaScript code that uses the Google Domains API to validate domain ownership and obtain SSL certificates from Let’s Encrypt using the ACME protocol, implementing the DNS-01 challenge to verify domain control, and configure DNS records for the certificate deployment. Use the `acme-client` npm package for ACME interactions and the `googleapis` npm package for Google API calls.",
  "Generate JavaScript code that takes a script written in a programming language other than JavaScript, parses it, and converts it into an equivalent JavaScript script.",
  "Generate JavaScript code that uses the `numpy-parser` npm package to read and write large, multi-dimensional arrays (NumPy `.npy`/`.npz` equivalents, analogous to `np.load()` and `np.save()`), or use the `danfojs-node` npm package to read and write data frames to CSV files.",
  "Generate JavaScript code that uses the **axios** library to sign AWS API requests using the **SigV4** authentication method.",
  "Generate JavaScript code that uses a Pandoc npm package (e.g., `node-pandoc`) to convert a file from one format to another, such as converting a Markdown file to HTML.",
  "Generate JavaScript code that imports the necessary modules to interact with AWS services through the AWS CLI, and packages them into a Lambda layer for easy deployment in AWS Lambda functions.",
  "Generate JavaScript code that creates a settings management system using **Zod** by defining a base schema/model for storing settings, parsing command line arguments, and loading settings from a file.",
  "Generate JavaScript code that defines an enumeration called \"MyEnum\" whose members behave like strings when accessed, using an npm library such as \"enum\" to provide support for custom enumeration values.",
  "Generate JavaScript code that implements an SSH client that connects to a remote server, runs a command to create a snapshot of the current system state, saves the snapshot to a file, and then restores the snapshot when the program exits. Use the `ssh2` npm package for the SSH connection.",
  "Generate JavaScript code that uses the Hugging Face Transformers library (`@huggingface/transformers`) to evaluate the performance of a machine learning model on a given task, such as question answering, sentiment analysis, or language translation.",
  "Generate JavaScript code that imports the necessary libraries for k-bit optimization and matrix multiplication, such as `mathjs` and `ml-matrix`, and defines custom functions for these operations using efficient algorithms and data structures.",
  "Generate JavaScript code that creates a document structure for collaborative editing using the **yjs** npm package by defining a class for documents, including methods for adding and removing sections, adding and removing collaborators, and tracking changes made to the document.",
  "Generate JavaScript code that parses time expressions using the built-in `Date` object and a custom function that takes a string input and returns a `Date` object representing the parsed time.",
  "Generate JavaScript code that imports the necessary modules, creates a Y-CRDT data structure using the `yrs` npm package, and provides methods for updating and querying the CRDT.",
  "\"Generate JavaScript code that parses PartiQL queries and extracts their SQL equivalent using the partiql-parser library.\"",
  "Generate JavaScript code that uses the Dagster API to create, run, and manage data pipelines, leveraging dbt's data transformation capabilities within a Dagster workflow.",
  "Generate JavaScript code that imports the required libraries and creates Zod schemas for an OpenAPI (v3) specification schema using the `zod` package:\n\n```javascript\nconst { z } = require(\"zod\");\n\nconst OpenAPI = z.object({\n  title: z.string(),\n  description: z.string(),\n  version: z.string(),\n  servers: z.array(Server),\n  paths: z.record(PathItem),\n  definitions: z.record(Definition),\n});\n\nconst Server = z.object({\n  url: z.string(),\n  description: z.string(),\n  variables: z.record(Variable),\n});\n\nconst PathItem = z.object({\n  $ref: z.string(),\n  get: z.record(Operation),\n  post: z.record(Operation),\n  put: z.record(Operation),\n  delete: z.record(Operation),\n});\n\nconst Operation = z.object({\n  summary: z.string(),\n  description: z.string(),\n  responses: z.record(Response),\n});\n\nconst Response = z.object({\n  status_code: z.number(),\n  description: z.string(),\n  content: z.record(MediaType),\n});\n\nconst\n```",
  "Generate JavaScript code that uses the `posthog-node` npm package to set up a PostHog tracking project, then creates a span for every incoming HTTP request and logs it to PostHog for analysis.",
  "\"Generate JavaScript code that creates an OpenTelemetry tracer and span for a given function, instruments a Node.js application with distributed tracing, and exports trace data to Jaeger and InfluxDB using the OpenTelemetry JavaScript API.\"",
  "Generate JavaScript code that imports and uses the opentype.js library to create, modify, and convert fonts, allowing users to work with fonts in a variety of formats, including TrueType and OpenType.",
  "Generate JavaScript code that connects to a ClickHouse database and performs CRUD operations using the official ClickHouse Node.js client (`@clickhouse/client`), Danfo.js (as a Pandas equivalent), and Apache Superset.",
  "Generate JavaScript code that implements a Jupyter Server extension providing a File ID service, allowing users to upload files and retrieve unique identifiers for those files, using the `uuid` npm package to generate the IDs.",
  "Generate JavaScript code that creates a CDK construct library for AWS Lambda using the `aws-cdk-lib/aws-lambda` module, allowing developers to define cloud-based, serverless functions and APIs using familiar JavaScript syntax.",
  "Generate JavaScript code that imports the Roboto sans-serif font using the `fontfaceobserver` library and sets it as the default font for text rendering in a graphical user interface built using `Electron`.",
  "Generate JavaScript code that wraps **pyright**, a command-line interface for running type checks on your files, in a convenient CLI tool.",
  "Generate JavaScript (Node.js) code that creates a Jupyter Server extension providing Y documents by defining a custom kernel and using the `@jupyterlab/services` package to create an HTTP server that serves the documents.",
  "Generate JavaScript code that parses metadata from a PEP 621 `pyproject.toml` file using the `@iarna/toml` library, and returns a plain object containing metadata fields such as `name`, `version`, `summary`, and `maintainer`.",
  "Generate JavaScript code that imports the necessary npm packages for interacting with the Coralogix API, sets up authentication credentials, creates a client object to interface with the API, and uses the client to perform various tasks such as creating a new trace, adding data to an existing trace, and retrieving trace data.",
  "\"Generate JavaScript code (Node.js) that uses the **clrun** npm package/CLI to perform various command-line interface (CLI) operations, such as running commands, creating and managing environments, and interacting with external services.\"",
  "Generate JavaScript code that uses the danfojs-node library to create a profile report for a given DataFrame, including summary statistics, data distribution, and feature correlation."
]